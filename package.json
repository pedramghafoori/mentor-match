{
  "name": "mentor-match",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n BACKEND,FRONTEND -c yellow,cyan \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
    "dev:back": "npm run dev --prefix backend",
    "dev:front": "npm start --prefix frontend"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "devDependencies": {
    "@types/pg": "^8.11.14",
    "@types/react": "^19.1.2",
    "@types/react-dom": "^19.1.2",
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.15.6"
  }
}
